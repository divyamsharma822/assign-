import React from "react";
import "./Card.scss";

const Card = ({ emoji, heading, detail }) => {
    return (
       <div className="card">
        <div className="hi"
       </div>
    );
};

export default Card;
